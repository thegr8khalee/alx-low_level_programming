/**
 * _atoi - gives the int value of a string
 * @c: pointer to the string
 * return: int value of the string
 */
int _atoi(char *s)
{
	if (s[] <= '0' && s[] >= '0')
		return (s);
	else
		return (0);
}
